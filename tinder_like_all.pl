#!/opt/local/bin/perl -W
use strict;
use warnings;
use Data::Dump qw(dump);
#use POSIX qw(strftime);
#use Getopt::Long;
#

my $TINDER_TOKEN = "x";
my $XAUTHTOKEN = "x";
my $TINDER_ID = "x";

die if !$TINDER_ID || !$XAUTHTOKEN || !$TINDER_ID;

print "Hello all\n";


exit 0;
